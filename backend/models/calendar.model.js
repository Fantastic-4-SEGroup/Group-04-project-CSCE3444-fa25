// Placeholder for Calendar model

const CalendarSchema = {
  user_id: 'string', // or ObjectId
  date: 'date',
  mood: 'string',
};

// Mock data
const calendarEntries = [];

module.exports = { CalendarSchema, calendarEntries };
